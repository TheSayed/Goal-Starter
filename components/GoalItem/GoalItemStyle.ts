import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  listItem: {
    backgroundColor: "#BB7EF5",
    height: 50,
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 20,
    marginBottom: 10,
  },
  listText: {
    color: "white",
  },
  pressedItem: {
    backgroundColor: "green",
  },
});
